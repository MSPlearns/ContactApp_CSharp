<UserControl x:Class="Presentation.WinPF_App.Views.NewContactView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Presentation.WinPF_App.Views"
             xmlns:viewmodels="clr-namespace:Presentation.WinPF_App.ViewModels" 
             d:DataContext="{d:DesignInstance Type=viewmodels:NewContactViewModel}"
             mc:Ignorable="d" 
             Background="White"
             d:DesignHeight="450" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="15"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="15"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="15"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="2" Grid.Row="1" Text="New Contact" FontSize="28" TextAlignment="Center"/>

        <StackPanel Grid.Column="2" Grid.Row="2" Orientation="Vertical" Margin="0 10 0 0">
            <Grid Margin="0 0 0 10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Column="0" Grid.Row="0" Text="First Name*" Margin="0 0 0 5"/>
                <TextBox Text="{Binding CcForm.FirstName}" Grid.Column="0" Grid.Row="1"  Padding="4" Margin="0 0 5 0">
                   
                </TextBox>
                
                <TextBlock Grid.Column="1" Grid.Row="0" Text="Last Name*" Margin="5 0 0 5"/>
                <TextBox Text="{Binding CcForm.LastName}" Grid.Column="1" Grid.Row="1" Padding="4" Margin="5 0 0 0">
                    
                </TextBox>
            </Grid>

            <TextBlock Text="Email**"/>
            <TextBox Text="{Binding CcForm.Email}" Margin="0, 0, 0, 10" Padding="4">
               
            </TextBox>

            <TextBlock Text="Phone number**"/>
            <TextBox Text="{Binding CcForm.PhoneNumber}" Margin="0, 0, 0, 10" Padding="4">
             
            </TextBox>

            <TextBlock Text="Address"/>
            <TextBox Text="{Binding CcForm.Address}" Height="auto" MinHeight="40" Margin="0, 0, 0, 10" Padding="4">
              
            </TextBox>

            <TextBlock Text="Postcode"/>
            <TextBox Text="{Binding CcForm.Postcode}" Margin="0, 0, 0, 10" Padding="4">
               
            </TextBox>

            <TextBlock Text="City"/>
            <TextBox Text="{Binding CcForm.City}" Margin="0, 0, 0, 10" Padding="4">
              
            </TextBox>

        </StackPanel>

        <Grid Grid.Column="2" Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0"  Content="Save Contact" Padding="5" Margin="2.5" Command="{Binding SaveContactCommand}"/>
            <Button Grid.Column="1"  Content="Cancel" Padding="5" Margin="2.5" Command="{Binding GoToContactsCommand}"/>
        </Grid>
        
    </Grid>
</UserControl>
