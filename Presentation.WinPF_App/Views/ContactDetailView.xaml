<UserControl x:Class="Presentation.WinPF_App.Views.ContactDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Presentation.WinPF_App.Views"
             xmlns:viewmodels="clr-namespace:Presentation.WinPF_App.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:ContactDetailViewModel}"
             mc:Ignorable="d" 
                Background="White"
             d:DesignHeight="450" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="15"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="15"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="15"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="2" Grid.Row="1" Text="{Binding SelectedContact.FullName}" FontSize="28" TextAlignment="Center"/>

        <StackPanel Grid.Column="2" Grid.Row="2" Orientation="Vertical" Margin="0 10 0 0">
           
            <TextBlock Text="{Binding SelectedContact.Email}" Margin="0, 0, 0, 10" Padding="4"/>

            <TextBlock Text="{Binding SelectedContact.PhoneNumber}" Margin="0, 0, 0, 10" Padding="4"/>

            <TextBlock Text="{Binding SelectedContact.Address}" Height="auto" MinHeight="40" Margin="0, 0, 0, 10" Padding="4"/>

            <TextBlock Text="{Binding SelectedContact.Postcode}" Margin="0, 0, 0, 10" Padding="4"/>

            <TextBlock Text="{Binding SelectedContact.City}" Margin="0, 0, 0, 10" Padding="4"/>

        </StackPanel>

        <Grid Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Grid.Row="0" Content="Edit Contact" Padding="5" Command="{Binding EditContactCommand}" CommandParameter="{Binding SelectedContact}"/>
            <Button Grid.Column="1" Grid.Row="0" Content="Delete Contact" Padding="5" Command="{Binding DeleteContactCommand}" CommandParameter="{Binding SelectedContact}"/>
            <Button Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" Content="Return" Padding="5" Command="{Binding GoToContactsCommand}"/>
        </Grid>
    </Grid>
</UserControl>
    
